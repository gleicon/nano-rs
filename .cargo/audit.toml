[advisories]
# Ignore advisories for transitive deps with no available patch
ignore = [
    # paste - unmaintained, no patch (RUSTSEC-2024-0436)
    { id = "RUSTSEC-2024-0436", reason = "transitive dep, unmaintained but not exploitable in this context" },
    # rsa - Marvin Attack, transitive dep via rustls/webpki (RUSTSEC-2023-0071)
    { id = "RUSTSEC-2023-0071", reason = "transitive dep, no direct RSA PKCS1v1.5 decryption in nano-rs" },
    # quick-xml (RUSTSEC-2026-0194)
    { id = "RUSTSEC-2026-0194", reason = "transitive dep, not directly used" },
]
